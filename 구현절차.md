# 구현 절차

## Phase 1 — 개발 환경 + CAN 버스 통신 검증 

**목표:** STM32 두 보드가 CAN으로 실제 통신하는 것 확인

- STM32CubeIDE 프로젝트 생성 (F446RE ×2, DriveECU / SensorECU)
- 하드웨어 연결

```
STM32 #1 --- SN65HVD230 ---[120Ω]--- SN65HVD230 --- STM32 #2
                               |
                           CANable --- PC (USB)
```

- CAN 핀: PA11 (CAN1_RX), PA12 (CAN1_TX)
- 버스 양 끝단에 120Ω 종단저항 각 1개
- PC 측 툴 설치

### 하드웨어 배선 절차

**각 Nucleo → SN65HVD230 연결 (×2)**

| Nucleo 핀 | SN65HVD230 핀 | 비고 |
|---|---|---|
| PA12 (CAN1_TX) | CTX | 데이터 송신 |
| PA11 (CAN1_RX) | CRX | 데이터 수신 |
| 3.3V | VCC | 트랜시버 전원 |
| GND | GND | 공통 그라운드 |

**CAN 버스 (브레드보드)**

```
DriveECU                         SensorECU
SN65HVD230                       SN65HVD230
  CANH ─┬────────────────────────┬─ CANH
  CANL ─┼────────────────────────┼─ CANL
        │                        │
       [120Ω]              [120Ω]  ← 버스 양 끝단 종단저항
        │                        │
       GND                      GND
```

**CANable 연결**

| CANable 단자 | 연결 대상 |
|---|---|
| CANH | 버스 CANH 라인 |
| CANL | 버스 CANL 라인 |
| GND | 공통 GND |
| USB | PC |

**GND 공통 연결 필수**
- DriveECU GND, SensorECU GND, CANable GND, 벤치파워 GND 모두 브레드보드 GND 레일에 연결

**전원 인가 순서**
1. 벤치파워 5V → SensorECU E5V 핀 (JP5를 2-3 위치로 변경 후)
2. USB → DriveECU (PC 연결)
3. CANable USB → PC

**정상 동작 확인**
- 두 보드 모두 LD2(녹색) 깜빡임 → CAN 메시지 송신 중
- 각 보드 UART 터미널에 상대방 ID 수신 로그 출력



```bash
pip install python-can   # CANable → slcan 인터페이스로 인식
```

- CAN Loopback 테스트 (단일 보드 자가 송수신) → 두 노드 양방향 통신 테스트 → CANable PC 모니터링
- 오실로스코프로 CAN_H/CAN_L 파형 확인 (500Kbps 검증)

---

## Phase 2 — CAN 메시지 구조 구현 

**목표:** SRS Section 13.2 CAN ID 테이블대로 메시지 송수신

| 보드 | 역할 | 구현 내용 |
|---|---|---|
| STM32 #1 | Drive ECU | 0x100 Status 메시지 200ms 주기 송신 |
| STM32 #2 | Sensor ECU | 0x201 Heartbeat 100ms 주기 송신 |
| PC (Python) | 임시 Gateway | 수신 파싱 + UDS 명령 송신 |

---

---

## Phase 3 — Bootloader 구현 (4~6일)

**목표:** 전원 인가 시 유효한 Slot을 선택해 점프하는 Bootloader

- Flash 레이아웃 설계 (F446RE 512KB 기준)

```
Sector 0~1: Bootloader (32KB)
Sector 2~3: Slot A Application
Sector 4~5: Slot B Application
Sector 6:   Metadata (Confirmed/Pending/BootCount)
```

- Metadata 구조체 정의 → EEPROM(24LC256) 또는 Flash Sector 6
- 부팅 로직: Confirmed Slot 확인 → 서명 검증 → 점프
- IWDG 8000ms 설정 → 애플리케이션이 kick하지 않으면 리셋
- WRP(Write Protection) Sector 0~1 활성화
- Safe State 진입 조건 구현 (검증 실패 시 기존 Slot 유지 + 대기)

---

## Phase 4 — UDS + ISO-TP + OTA 프로토콜 (5~7일)

**목표:** PC Python 스크립트로 STM32에 새 펌웨어를 OTA 업로드

- ISO-TP 구현 (STM32 측 수신 처리)
  - Single Frame / First Frame / Consecutive Frame
  - STmin=0, BlockSize=0, N_Bs=1000ms
- UDS 서비스 구현 (STM32)
  - `0x10` DiagnosticSessionControl
  - `0x27` SecurityAccess (HMAC-SHA256 Seed/Key)
  - `0x34` RequestDownload
  - `0x36` TransferData (256 byte 청크)
  - `0x37` RequestTransferExit
- PC Python UDS 클라이언트 (python-can + python-uds 라이브러리)
- E2E 테스트: PC → CANable → STM32 펌웨어 업로드 → 재부팅 → 새 슬롯 실행 확인

---

## Phase 5 — ECDSA 펌웨어 서명 + Jenkins (3~4일)

**목표:** 서명된 펌웨어만 Bootloader가 수락

- 키 생성

```bash
openssl ecparam -name prime256v1 -genkey -out private_key.pem
openssl ec -in private_key.pem -pubout -out public_key.pem
```

- Public key → Bootloader Flash에 하드코딩
- 빌드 후 서명 스크립트 (Python + cryptography 라이브러리)
- Bootloader 서명 검증 로직 (mbedTLS 또는 micro-ecc)
- Jenkins 파이프라인 구성 (SRS FR-CICD-001~010)
  - 크로스 컴파일 → cppcheck → 서명 → manifest 생성 → OTA 배포

---

## Phase 6 — RPi5 + RC카 통합 (6차 구매 도착 후)

- RPi5 OS 설치 + CAN 인터페이스 설정
- RC카 프레임 조립 + 모터 드라이버 배선
- Drive ECU 모터 제어 로직 + 라인트레이서 센서 통합
- Sensor ECU HC-SR04 장애물 감지
- 전체 E2E 시나리오 테스트
